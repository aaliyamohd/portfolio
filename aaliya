<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mohd Aaliya Jabeen - Portfolio</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            cursor: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='40' height='48' viewport='0 0 100 100' style='fill:black;font-size:24px;'><text y='50%'>🦋</text></svg>") 16 0, auto;
        }

        body {
            background: linear-gradient(135deg, #ffe5f0 0%, #fff6e5 100%);
            font-family: 'Arial', sans-serif;
            min-height: 100vh;
            overflow-x: hidden;
        }

        .butterfly {
            position: fixed;
            pointer-events: none;
            z-index: -1;
            opacity: 0.4;
            transform-origin: center;
            animation: flyAround 20s linear infinite;
        }

        @keyframes flyAround {
            0% {
                transform: translate(0, 0) rotate(0deg) scale(1);
            }
            25% {
                transform: translate(100px, -100px) rotate(45deg) scale(1.1);
            }
            50% {
                transform: translate(200px, 0) rotate(90deg) scale(1);
            }
            75% {
                transform: translate(100px, 100px) rotate(45deg) scale(0.9);
            }
            100% {
                transform: translate(0, 0) rotate(0deg) scale(1);
            }
        }

        .butterfly-wings {
            animation: flapWings 0.5s ease-in-out infinite alternate;
        }

        @keyframes flapWings {
            0% {
                transform: scaleX(1);
            }
            100% {
                transform: scaleX(0.8);
            }
        }

        .flower {
            position: fixed;
            pointer-events: none;
            z-index: -1;
            opacity: 0.3;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
            position: relative;
            z-index: 1;
        }

        header {
            text-align: center;
            padding: 3rem 0;
            background: rgba(255, 255, 255, 0.9);
            border-radius: 20px;
            margin-bottom: 2rem;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        h1 {
            font-size: 3rem;
            color: #ff69b4;
            margin-bottom: 1rem;
        }

        .contact {
            font-size: 1.2rem;
            color: #666;
        }

        .projects {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }

        .project-card {
            background: rgba(255, 255, 255, 0.9);
            padding: 1.5rem;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        .project-card:hover {
            transform: translateY(-5px);
        }

        .achievements {
            margin-top: 2rem;
            background: rgba(255, 255, 255, 0.9);
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        .contact-section {
            margin-top: 2rem;
            background: rgba(255, 255, 255, 0.9);
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .contact-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-top: 1.5rem;
        }

        .contact-item {
            padding: 1rem;
            background: rgba(255, 192, 203, 0.1);
            border-radius: 10px;
            transition: transform 0.3s ease;
        }

        .contact-item:hover {
            transform: translateY(-3px);
        }

        .contact-icon {
            font-size: 2rem;
            margin-bottom: 0.5rem;
        }

        h2 {
            color: #ff69b4;
            margin-bottom: 1rem;
        }

        ul {
            list-style: none;
        }

        li {
            margin-bottom: 0.5rem;
            color: #666;
        }

        li::before {
            content: "🦋";
            margin-right: 0.5rem;
        }

        a {
            color: #ff69b4;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        a:hover {
            color: #ff1493;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Mohd Aaliya Jabeen</h1>
            <p class="contact">Contact: 73866*****</p>
        </header>

        <section class="projects">
            <div class="project-card">
                <h2>Portfolio Website</h2>
                <p>A responsive portfolio website showcasing my work and achievements. Built using HTML, CSS, and JavaScript with interactive elements and animations.</p>
            </div>
            <div class="project-card">
                <h2>E-commerce Website</h2>
                <p>Developed a full-featured e-commerce platform with product catalog, shopping cart, and secure payment integration.</p>
            </div>
            <div class="project-card">
                <h2>Weather App</h2>
                <p>Created a weather application that provides real-time weather updates using API integration and modern JavaScript.</p>
            </div>
        </section>

        <section class="achievements">
            <h2>Achievements</h2>
            <ul>
                <li>Full Stack Web Development Certification</li>
                <li>Winner of College Web Design Competition 2024</li>
                <li>Published Research Paper on Modern Web Technologies</li>
                <li>Led Student Web Development Club</li>
                <li>Completed 10+ Client Projects Successfully</li>
            </ul>
        </section>

        <section class="contact-section">
            <h2>Get in Touch</h2>
            <div class="contact-grid">
                <div class="contact-item">
                    <div class="contact-icon">📱</div>
                    <h3>Phone</h3>
                    <p>73866*****</p>
                </div>
                <div class="contact-item">
                    <div class="contact-icon">📧</div>
                    <h3>Email</h3>
                    <p><a href="mailto:mohd.aaliya@example.com">mohd.aaliya@example.com</a></p>
                </div>
                <div class="contact-item">
                    <div class="contact-icon">🌍</div>
                    <h3>Location</h3>
                    <p>Hyderabad, India</p>
                </div>
                <div class="contact-item">
                    <div class="contact-icon">💼</div>
                    <h3>Portfolio</h3>
                    <p><a href="#" target="_blank">www.aaliyajabeen.com</a></p>
                </div>
            </div>
        </section>
    </div>

    <script>
        const butterflyColors = [
            { emoji: '🦋', color: '#89CFF0' },  // Blue
            { emoji: '🦋', color: '#FFB6C1' },  // Pink
            { emoji: '🦋', color: '#90EE90' },  // Light green
            { emoji: '🦋', color: '#DDA0DD' },  // Plum
            { emoji: '🦋', color: '#FFD700' },  // Gold
            { emoji: '🦋', color: '#FF69B4' }   // Hot pink
        ];

        function createButterfly() {
            const butterfly = document.createElement('div');
            const butterflyWings = document.createElement('div');
            const randomButterfly = butterflyColors[Math.floor(Math.random() * butterflyColors.length)];
            
            butterflyWings.innerHTML = randomButterfly.emoji;
            butterflyWings.className = 'butterfly-wings';
            butterfly.appendChild(butterflyWings);
            butterfly.className = 'butterfly';
            
            butterfly.style.fontSize = Math.random() * 15 + 15 + 'px';
            butterfly.style.left = Math.random() * window.innerWidth + 'px';
            butterfly.style.top = Math.random() * window.innerHeight + 'px';
            
            const duration = Math.random() * 10 + 15;
            butterfly.style.animationDuration = `${duration}s`;
            butterfly.style.animationDelay = `-${Math.random() * duration}s`;
            
            const randomPath = Math.floor(Math.random() * 4);
            butterfly.style.setProperty('--fly-path', randomPath);
            
            butterfly.style.color = randomButterfly.color;
            butterfly.style.filter = `drop-shadow(0 0 3px ${randomButterfly.color})`;
            
            document.body.appendChild(butterfly);

            setTimeout(() => {
                butterfly.remove();
            }, duration * 1000);
        }

        function createFlower() {
            const flowers = ['🌹', '🌸', '🌻'];
            const flower = document.createElement('div');
            flower.innerHTML = flowers[Math.floor(Math.random() * flowers.length)];
            flower.className = 'flower';
            flower.style.fontSize = Math.random() * 15 + 15 + 'px';
            flower.style.left = Math.random() * window.innerWidth + 'px';
            flower.style.top = Math.random() * window.innerHeight + 'px';
            document.body.appendChild(flower);
        }

        // Create initial butterflies and flowers
        for (let i = 0; i < 20; i++) {
            createButterfly();
            if (i < 10) createFlower();
        }

        // Add new butterflies periodically
        setInterval(createButterfly, 2000);

        // Add butterflies on page load and refresh
        window.addEventListener('load', () => {
            for (let i = 0; i < 10; i++) {
                setTimeout(createButterfly, i * 300);
            }
        });

        // Handle window resize
        let resizeTimeout;
        window.addEventListener('resize', () => {
            clearTimeout(resizeTimeout);
            resizeTimeout = setTimeout(() => {
                document.querySelectorAll('.butterfly').forEach(b => b.remove());
                for (let i = 0; i < 20; i++) {
                    createButterfly();
                }
            }, 250);
        });
    </script>
</body>
</html>
